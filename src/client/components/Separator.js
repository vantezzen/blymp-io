import React from 'react';

const Separator = () => (
  <div style={{ 
    margin: '3rem 0',
    display: 'flex',
    justifyContent: 'center',
  }}>
    <div style={{
      width: '70vw',
      maxWidth: 500,
      height: 3,
      backgroundColor: '#EBEBEB',
      borderRadius: 1
    }} />
  </div>
);

export default Separator;
